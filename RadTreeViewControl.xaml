<UserControl
    x:Class="RadTreeView.RadTreeViewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RadTreeView"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Margin="4"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:ColumnViewModel}">
            <Grid Cursor="Hand" Tag="{Binding}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2" />
                </Grid.ColumnDefinitions>

                <Border
                    Grid.Column="0"
                    Padding="2"
                    BorderBrush="LightGray"
                    BorderThickness="1">
                    <Border BorderBrush="LightGray" BorderThickness="1">
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="14"
                            FontWeight="Medium"
                            Text="{Binding Title}" />
                    </Border>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:RowViewModel}">
            <Border DataContext="{Binding}" Loaded="Border_Loaded" />
        </DataTemplate>

    </UserControl.Resources>


    <Border
        x:Name="MainBorder"
        Margin="2"
        Padding="1"
        BorderBrush="LightGray"
        BorderThickness="2">
        <Grid
            x:Name="PART_RootGrid"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="White"
            Loaded="PART_RootGrid_Loaded"
            SnapsToDevicePixels="True" />
    </Border>

</UserControl>
